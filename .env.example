# Copy to .env and fill in when ready. Do not commit .env.
# .env is gitignored.

# -----------------------------------------------------------------------------
# Claude (Claude Code / Anthropic API)
# Get keys: https://console.anthropic.com
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=

# Optional: model override (default depends on Claude Code / app)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# -----------------------------------------------------------------------------
# Moltbot (OpenClaw) — OpenClaw uses its own auth store, not .env.
# After install: openclaw onboard && openclaw agents add
# Auth lives in ~/.openclaw/agents/<id>/agent/auth-profiles.json
# Set workspace in ~/.openclaw/openclaw.json:
#   "agents": { "defaults": { "workspace": "/path/to/Metadata-Upload" } }
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# GitHub (for the Python watcher scripts)
# -----------------------------------------------------------------------------
# GITHUB_TOKEN=   # used by auto_commit_watcher.py
# REPO_TOKEN=     # used by file_watcher_github.py and network_file_watcher_github.py
